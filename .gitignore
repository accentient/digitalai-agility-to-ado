# Local configuration containing tokens and instance details
appsettings.json

# Instance-specific value maps (copy mappings.sample.json to mappings.json)
mappings.json

# PowerShell / editor
*.suo
*.user
.vs/
.vscode/
TestResults/
*.log

# Run artifacts. The whole folder, not just *.log: runs also drop id lists and
# other scratch files here, and one of those was tracked by accident.
logs/

# OS
Thumbs.db
.DS_Store

# Local notes and internal working docs (README stays public)
/*.md
!README.md
